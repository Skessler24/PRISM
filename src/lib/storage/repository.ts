/** Storage repository — Local (default) + Graph stub. Prefer importing from `./index`. */
export { storage, createStorageRepository, LocalStorageRepository, GraphOneDriveRepository } from './index'
export type { StorageRepository } from './types'
