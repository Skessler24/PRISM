/** Placeholder types — full DistrictProfile model lands in Prompt 2. */
export type DistrictProfileStub = {
  id: string
  name: string
  state: string
  iepSystem: string
}

export const ACTIVE_PROFILE_ID = 'cherry-creek'
