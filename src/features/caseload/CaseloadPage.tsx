import { PageShell } from '../../components/PageShell'

export function CaseloadPage() {
  return (
    <PageShell
      title="👤 My Caseload"
      description="Personal caseload management, session tracking, and SOAP note generation per provider."
    />
  )
}
