import { PageShell } from '../../components/PageShell'

export function TemplatesPage() {
  return (
    <PageShell
      title="🎨 Templates & Forms"
      description="Forms library, template creator, FBA/BIP engine, icon library, token boards, schedules, and comm boards."
    />
  )
}
